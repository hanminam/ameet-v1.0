# Web Framework & Server
fastapi
uvicorn[standard]
gunicorn

# Configuration
pydantic
pydantic-settings
python-dotenv

# Database ORM / Drivers
sqlalchemy
aiomysql  # MariaDB (Async)
redis>=4.2
beanie    # MongoDB (Async ODM)
motor     # MongoDB (Async Driver)

# Google Cloud
# cloud-sql-python-connector[asyncio]
cloud-sql-python-connector==1.18.4

# LangChain & Tools (향후 Phase에서 사용)
langchain
langgraph
langchain-google-genai
langchain-openai
langchain-anthropic
langchain-community
tavily-python

# PDF Generation (향후 Phase에서 사용)
playwright